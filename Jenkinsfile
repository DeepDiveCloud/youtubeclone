pipeline {
    agent any

    stages {
        stage("check version")  {
            steps {
                sh "node --version"
                sh "npm --version"
            }
        }
        stages {
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        
    }
}
